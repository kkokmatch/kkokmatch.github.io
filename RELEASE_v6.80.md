# v6.80 — 편성대기 예상 시작

기준: main `ec10da7f5f165dd5f795ff7c04912edab77f7042` (v6.79).
기존 운영본은 `backup/v6.79` 브랜치와 `versions/v6.79/`에 보존한다.

## 동작

- 4명이 완성된 편성대기 카드에 `예상 시작 약 N분 후`와 한국시간 `예상 HH:MM 시작`을 표시한다. 빈 코트로 바로 배정 가능한 조는 `예상 곧 시작 가능`으로 표시한다.
- 진행 중인 코트별 잔여시간을 계산하고, 완성된 대기조 순서대로 가장 빨리 비는 코트에 가상 배정한다. 미완성 조는 코트 시간을 점유하지 않는다. 실제 경기 시작·코트 선택·자동편성·저장 동작은 바꾸지 않는다.
- 현재 모임의 `S.history`에서 한국시간 오전 5시 기준 당일 종료 기록을 사용한다. `startedAt`/`endedAt`을 우선하고, 시작값이 없으면 `durationMin`을 사용한다. 중복·미래·비정상 기록 및 자동종료 기록, 3분 미만/30분 이상 기록은 제외한다.
- 기본값은 동일급수 또는 A/B가 3명 이상이면 17.5분, 나머지 혼합급수는 12.5분, 급수 미확인은 15분이다.
- 당일 평균 40% + 최근 8경기 평균 60%를 사용하고, 비슷한 급수 구성의 최근 12경기까지 추가 반영한다. 유효 기록 8개에서 실측값 비중이 100%가 되며, 그 전에는 기본값과 혼합한다. 표본 5개부터 중앙값 기반으로 이상치 영향을 제한한다.
- 진행 중인 경기가 평균보다 길어지면 아직 종료되지 않은 표본의 잔여시간을 참고하며, 종료 확인 전에는 최소 1분으로 표시한다. 이는 보장 시간이 아닌 추정치다.
- 기존 15초 갱신 주기, 화면 복귀, 상태 부분 갱신 및 전체 렌더 경로에서 표시를 갱신한다. 시간만 변경할 때는 카드 전체를 재생성하지 않는다.

## 검증

`node scripts/qa-pending-eta.mjs`는 현재 버전 파일을 읽어 계산과 Chromium 1280/390px, WebKit 390/375px 화면을 검사한다. 복수 코트·순서 변경·경기 종료·인원 부족·데이터 fallback·당일 실측 우선·오전 5시 경계·자동종료 제외·시간 경과·모임 변경·레이아웃·콘솔 오류·단일 버전 자산을 검증한다.

기존 QA: 단일 런타임 구조, 로그인, 회원 상태전환/동명이인/수정 저장, 투표/파트너, 자동편성/수동 충돌, 역할별 레이아웃, 운동통계, iPhone PWA 자산 실패 복구/캐시 업데이트. 구형 QA의 설치 안내 차단 및 저장 API mock 보정은 기존 CI의 준비 절차와 동일하게 적용한다.

실측 운영 데이터에 대한 예측 오차 개선율은 아직 측정하지 않았다. QA는 격리된 fixture를 사용하며 실제 모임 기록을 변경하지 않는다. iPhone 검증은 WebKit과 모바일 화면 모사이며 실제 기기 검증은 별도다.
