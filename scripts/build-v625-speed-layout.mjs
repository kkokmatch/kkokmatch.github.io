import fs from 'node:fs';
import cp from 'node:child_process';
const read=p=>fs.readFileSync(p,'utf8'),write=(p,s)=>fs.writeFileSync(p,s);
let js=read('app-v6.24.js');
const must=(oldText,newText,label)=>{if(!js.includes(oldText))throw new Error('missing patch target: '+label);js=js.replace(oldText,newText)};

// Version identity.
js=js.replaceAll("window.__kokmatchStandalone='6.24'","window.__kokmatchStandalone='6.25'")
     .replaceAll("window.__kokmatchVersionLock='6.24'","window.__kokmatchVersionLock='6.25'")
     .replaceAll("sessionStorage.setItem('kokmatch_runtime_version','6.24')","sessionStorage.setItem('kokmatch_runtime_version','6.25')");

// Remove the unnecessary Park-specific explanatory line from the canonical member editor.
must('<div class="note">박태영 카드와 동일한 회원정보 구조를 사용합니다.</div>','','member editor explanatory note');

// Save only the affected roster/header instead of rerendering every app view and running two roster post-processors.
const oldSave=`async function saveMemberEditorV618(ev){\n ev?.preventDefault?.();ev?.stopPropagation?.();const st=memberEditorStateV618,root=document.getElementById('memberEditorV615');if(!st||!root)return false;\n const name=String(root.querySelector('#v618Name')?.value||'').trim(),year=Number(root.querySelector('#v618Year')?.value||0),gender=String(root.querySelector('#v618Gender')?.value||'남'),cls=String(root.querySelector('#v618Cls')?.value||'C'),type=String(root.querySelector('#v618Type')?.value||st.type||'member');let role=String(root.querySelector('#v618Role')?.value||st.role||'member');if(type==='guest')role='member';const pin=String(root.querySelector('#v618Pin')?.value||'').trim(),inviter=type==='guest'?String(root.querySelector('#v618Inviter')?.value||'').trim():'';\n editorErrorV618('');if(!name){editorErrorV618('이름을 입력해주세요.');return false}if(!Number.isInteger(year)||year<1900||year>new Date().getFullYear()){editorErrorV618('출생연도를 확인해주세요.');return false}if(pin&&!/^\\d{4,8}$/.test(pin)){editorErrorV618('PIN/비밀번호는 숫자 4~8자리로 입력해주세요.');return false}if(type==='guest'&&!inviter){editorErrorV618('게스트의 초대인을 입력해주세요.');return false}\n const b=root.querySelector('#v618Save');if(b){b.disabled=true;b.textContent='저장 중...'}\n try{const x=await v615MemberApi('member_save',{memberId:st.memberId||'',name,year,gender,cls,type,role,pin,inviter});if(x?.data){S=x.data;window.S=x.data;try{normalizeClient()}catch{}}closeMemberEditorV618();renderHeader();renderNav();renderMembers();window.__kokmatchFinalizeRoster22?.();standardizeRosterV618();return true}catch(e){editorErrorV618(e?.message||String(e||'회원정보 저장에 실패했습니다.'));return false}finally{if(b?.isConnected){b.disabled=false;b.textContent=st.memberId?'저장':'등록'}}\n}`;
const newSave=`async function saveMemberEditorV618(ev){\n ev?.preventDefault?.();ev?.stopPropagation?.();const st=memberEditorStateV618,root=document.getElementById('memberEditorV615');if(!st||!root)return false;\n const name=String(root.querySelector('#v618Name')?.value||'').trim(),year=Number(root.querySelector('#v618Year')?.value||0),gender=String(root.querySelector('#v618Gender')?.value||'남'),cls=String(root.querySelector('#v618Cls')?.value||'C'),type=String(root.querySelector('#v618Type')?.value||st.type||'member');let role=String(root.querySelector('#v618Role')?.value||st.role||'member');if(type==='guest')role='member';const pin=String(root.querySelector('#v618Pin')?.value||'').trim(),inviter=type==='guest'?String(root.querySelector('#v618Inviter')?.value||'').trim():'';\n editorErrorV618('');if(!name){editorErrorV618('이름을 입력해주세요.');return false}if(!Number.isInteger(year)||year<1900||year>new Date().getFullYear()){editorErrorV618('출생연도를 확인해주세요.');return false}if(pin&&!/^\\d{4,8}$/.test(pin)){editorErrorV618('PIN/비밀번호는 숫자 4~8자리로 입력해주세요.');return false}if(type==='guest'&&!inviter){editorErrorV618('게스트의 초대인을 입력해주세요.');return false}\n const b=root.querySelector('#v618Save');if(b){b.disabled=true;b.textContent='저장 중...'}\n try{const x=await v620MemberApi('member_save',{memberId:st.memberId||'',name,year,gender,cls,type,role,pin,inviter,compact:true});if(x?.data){S=x.data;window.S=x.data;try{normalizeClient()}catch{}}else if(x?.member){const i=(S?.members||[]).findIndex(m=>String(m.id)===String(x.member.id));if(i>=0)S.members[i]=x.member;else S.members.push(x.member);window.S=S;try{normalizeClient()}catch{}}closeMemberEditorV618();renderHeader();if(role!==st.role||String(st.memberId||'')===String(me?.memberId||''))renderNav();renderMembers();requestAnimationFrame(()=>{try{window.__kokmatchFinalizeRoster22?.()}catch{}});return true}catch(e){editorErrorV618(e?.message||String(e||'회원정보 저장에 실패했습니다.'));return false}finally{if(b?.isConnected){b.disabled=false;b.textContent=st.memberId?'저장':'등록'}}\n}`;
must(oldSave,newSave,'canonical member save');

// Restore the previous calendar feel: circular days, poll dot, today ring, lighter past dates.
const oldDay=`for(let d=1;d<=last;d++){const dt=date623(y,m,d),sel=dt===selectedDate623,isToday=dt===today623(),withPoll=has.has(dt);cells.push(\`<button class="pollDay623 \${weekdayClass623(dt)} \${sel?'selected':''} \${isToday?'today':''} \${withPoll?'hasPoll':''}" onclick="selectPollDate623('\${dt}')"><span>\${d}</span></button>\`)}`;
const newDay=`for(let d=1;d<=last;d++){const dt=date623(y,m,d),sel=dt===selectedDate623,isToday=dt===today623(),withPoll=has.has(dt),past=dt<today623();cells.push(\`<button class="pollDay623 \${weekdayClass623(dt)} \${sel?'selected':''} \${isToday?'today':''} \${withPoll?'hasPoll':''} \${past?'past623':''}" onclick="selectPollDate623('\${dt}')"><span>\${d}</span></button>\`)}`;
must(oldDay,newDay,'canonical calendar day state');

// Exercise attendance poll must be the first block; today's statistics follow below.
const oldRenderPoll=`function renderPoll623(){const box=$('stats');if(!box)return;cleanupOldPollDom623();let wrap=box.querySelector('.pollWrap623');if(!wrap){wrap=document.createElement('div');wrap.className='pollWrap623';const recent=[...box.querySelectorAll(':scope > .card')].find(c=>(c.textContent||'').includes('오늘 최근 경기'));if(recent)recent.insertAdjacentElement('beforebegin',wrap);else box.appendChild(wrap)}wrap.innerHTML=section623()}`;
const newRenderPoll=`function renderPoll623(){const box=$('stats');if(!box)return;cleanupOldPollDom623();let wrap=box.querySelector('.pollWrap623');if(!wrap){wrap=document.createElement('div');wrap.className='pollWrap623'}if(box.firstElementChild!==wrap)box.prepend(wrap);wrap.innerHTML=section623()}`;
must(oldRenderPoll,newRenderPoll,'poll top placement');

// Poll forms/guest edits update only statistics, not every app screen.
for(const [from,to,label] of [
 ["await requestPoll623('poll_create',v);closeModal();renderAll();goView('stats')","await requestPoll623('poll_create',v);closeModal();renderStats();goView('stats')",'poll create render'],
 ["await requestPoll623('poll_update',{pollId:id,...v});closeModal();renderAll();goView('stats')","await requestPoll623('poll_update',{pollId:id,...v});closeModal();renderStats();goView('stats')",'poll update render'],
 ["await requestPoll623('poll_delete',{pollId:id});renderAll()","await requestPoll623('poll_delete',{pollId:id});renderStats()",'poll delete render'],
 ["await requestPoll623('poll_member_remove',{pollId:pid,memberId:mid});renderAll();openPollAttendees623(pid)","await requestPoll623('poll_member_remove',{pollId:pid,memberId:mid});renderStats();openPollAttendees623(pid)",'poll member remove render'],
 ["await requestPoll623('poll_guest_remove',{pollId:pid,guestId:gid});renderAll();openPollAttendees623(pid)","await requestPoll623('poll_guest_remove',{pollId:pid,guestId:gid});renderStats();openPollAttendees623(pid)",'poll guest remove render'],
 ["await requestPoll623('poll_guest_add',{pollId:id,name,year,gender,age,cls,inviter});closeModal();renderAll()","await requestPoll623('poll_guest_add',{pollId:id,name,year,gender,age,cls,inviter});closeModal();renderStats()",'poll guest add render']
 ])must(from,to,label);

js=js.replace('/* KokMatch v6.23 canonical exercise attendance poll runtime */','/* KokMatch v6.25 canonical exercise attendance poll runtime */');
write('app-v6.25.js',js);

let css=read('app-v6.24.css');
const marker='/* v6.24 canonical exercise attendance poll */';const at=css.indexOf(marker);if(at<0)throw new Error('canonical poll CSS marker missing');
const pollCss=`/* v6.25 canonical exercise attendance poll */\n.pollWrap623{margin:0 0 16px}.pollCalendar623{background:#fff;border:1px solid #e3e8f1;border-radius:17px;padding:11px 12px;margin-bottom:11px}.pollCalHead623{display:grid;grid-template-columns:40px 1fr 40px;align-items:center;text-align:center;margin-bottom:7px}.pollCalHead623 button{border:0;background:#eef3ff;color:#315dbd;border-radius:10px;min-height:34px;font-size:21px;font-weight:900}.pollCalHead623 button:disabled{opacity:.25}.pollWeek623,.pollGrid623{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:4px}.pollWeek623{margin-bottom:3px}.pollWeek623 span{text-align:center;font-size:11.5px;font-weight:900;color:#7b879e;padding:3px 0}.pollWeek623 span:first-child{color:#d45b67}.pollWeek623 span:last-child{color:#4774ce}.pollDay623,.pollBlank623{min-width:0;aspect-ratio:1;border:0}.pollDay623{position:relative;border-radius:50%;background:transparent;color:#27344d;font-size:14px;font-weight:900;display:grid;place-items:center;padding:0}.pollDay623.sun623{color:#d94646}.pollDay623.sat623{color:#2f67d8}.pollDay623.past623:not(.selected){color:#aab2c0}.pollDay623.past623.sun623:not(.selected){color:#df9aa2}.pollDay623.past623.sat623:not(.selected){color:#9aafe0}.pollDay623.today:not(.selected){box-shadow:inset 0 0 0 2px #9fb4ef;color:#244fb8}.pollDay623.hasPoll:not(.selected){background:#e9efff;color:#1747bf}.pollDay623.hasPoll:not(.selected):after{content:'';position:absolute;width:6px;height:6px;border-radius:50%;background:#e34f5f;bottom:3px;right:4px;box-shadow:0 0 0 2px #fff}.pollDay623.past623.hasPoll:not(.selected):after{background:#84cc66}.pollDay623.selected{background:var(--blue);color:#fff;box-shadow:0 3px 9px rgba(36,83,212,.24)}.pollDay623.selected.hasPoll:after{content:'';position:absolute;width:6px;height:6px;border-radius:50%;background:#ffd75d;bottom:3px;right:4px}.pollLegend623{display:flex;justify-content:space-between;align-items:center;gap:8px;font-size:10.5px;font-weight:750;color:#74809a;margin-top:8px}.pollLegend623 i{display:inline-block;width:7px;height:7px;border-radius:50%;background:#e34f5f;margin-right:5px}.pollHead623{display:flex;align-items:center;justify-content:space-between;gap:8px;margin:12px 0 8px}.pollCard623{margin-bottom:10px}.pollTitleRow623{display:flex;align-items:flex-start;justify-content:space-between;gap:8px}.pollMainTitle623{font-size:17px;font-weight:900;line-height:1.35}.pollSchedule623{font-size:12px;font-weight:700;color:#52627a;margin-top:4px}.pollCreator623{font-size:11px;color:#8290a4;margin-top:3px}.pollEnded623{display:inline-flex;font-size:10px;background:#edf0f4;color:#667085;border-radius:999px;padding:3px 6px;vertical-align:2px}.pollAdminBtns623{display:flex;gap:4px;flex:0 0 auto}.pollMini623{border:1px solid #ccd8e7;background:#fff;border-radius:8px;padding:6px 8px;font-size:11px;font-weight:800}.pollMini623.danger{color:#bd3d3d}.pollCounts623{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin:11px 0}.pollCounts623>div{background:#f5f8fc;border-radius:11px;padding:8px;text-align:center}.pollCounts623 b{display:block;font-size:16px}.pollCounts623 span{display:block;font-size:10px;color:#68788e;margin-top:2px}.pollActions623{display:grid;grid-template-columns:1fr 1fr;gap:6px}.pollAttendOn623{background:#e7f4ff!important;color:#17609c!important;border-color:#87b9e3!important}.pollGuestBtns623{display:flex;align-items:center;gap:6px;margin-top:7px}.pollGuestBtns623 .btn{flex:1}.pollFormGrid623{display:grid;grid-template-columns:1fr 1fr;gap:8px}.pollFormGrid623 .field{margin:0}.pollFormGrid623 .pollWide623{grid-column:1/-1}.pollPerson623{display:flex;align-items:center;justify-content:space-between;gap:8px;padding:9px 4px;border-bottom:1px solid #edf1f6}.pollPerson623>div{display:flex;align-items:center;gap:6px;flex-wrap:wrap}.pollPerson623 small{width:100%;padding-left:29px;color:#7a8799}.pollPerson623>button{border:0;background:#fbeaea;color:#b43d3d;width:28px;height:28px;border-radius:50%;font-weight:900}.pollGender623{display:inline-flex;width:22px;height:22px;align-items:center;justify-content:center;border-radius:50%;font-size:10px;font-weight:900}.pollGender623.male{background:#e6f0ff;color:#2e68a6}.pollGender623.female{background:#ffeaf1;color:#b64d73}.pollGrade623,.pollRole623{display:inline-flex;border-radius:999px;padding:3px 6px;font-size:10px;font-weight:900;background:#eef2f7;color:#44546b}.pollRole623.dev{background:#e8e8ff;color:#4b4bb0}.pollRole623.manager{background:#fff0d7;color:#94611d}.pollRole623.organizer{background:#e7f6ef;color:#287553}.pollRole623.guest{background:#f1ebff;color:#6c4ea3}.pollListSection623{margin-top:12px}.pollListHead623{display:flex;justify-content:space-between;align-items:center;margin-bottom:5px}.pollEmpty623{margin-top:8px}#stats>.title{margin-top:5px}#stats>.statsGrid{margin-bottom:8px}@media(max-width:430px){.pollCalendar623{padding:9px 10px}.pollWeek623,.pollGrid623{gap:2px}.pollDay623{font-size:13px}.pollFormGrid623{grid-template-columns:1fr}.pollFormGrid623 .pollWide623{grid-column:auto}.pollActions623{grid-template-columns:1fr 1fr}.pollMainTitle623{font-size:16px}}\n`;
css=css.slice(0,at)+pollCss;write('app-v6.25.css',css);

let index=read('index.html');index=index.replaceAll('6.24','6.25').replaceAll('v623','v625').replaceAll('manifest.webmanifest?v=6.23','manifest.webmanifest?v=6.25').replaceAll('app-v6.24.css?v=6.24','app-v6.25.css?v=6.25').replaceAll('app-v6.24.js?v=6.24','app-v6.25.js?v=6.25');write('index.html',index);
const latest=JSON.parse(read('latest-version.json'));latest.version=65;latest.label='v6.25';latest.semanticVersion='6.25';latest.build='v6.25';latest.updatedAt='2026-09-02T16:00:00+09:00';latest.note='v6.25 회원 저장 렌더링 경량화 · 회원등록 설명 정리 · 투표 저장 부분갱신 · 이전형 달력 디자인 · 참석투표 상단 배치';write('latest-version.json',JSON.stringify(latest,null,2)+'\n');

if(js.includes('박태영 카드와 동일한 회원정보 구조'))throw new Error('Park-specific member note remains');
if(!js.includes('compact:true'))throw new Error('compact member save flag missing');
if(js.includes("requestPoll623('poll_create',v);closeModal();renderAll()"))throw new Error('poll create still rerenders all views');
if(!js.includes("if(box.firstElementChild!==wrap)box.prepend(wrap)"))throw new Error('poll top placement missing');
cp.execFileSync('node',['--check','app-v6.25.js'],{stdio:'inherit'});cp.execFileSync('node',['scripts/validate-standalone-runtime.mjs'],{stdio:'inherit'});
console.log('PASS v6.25 build: targeted save rendering + restored poll calendar/layout');
