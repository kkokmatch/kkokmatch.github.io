# 콕매치 안정화 개발 원칙

## 1. 운영본 독립 원칙
- 운영 중인 버전은 자기 버전의 JS/CSS만 로드한다.
- 신버전이 구버전 `app-vXX.js`, `app-vXX.css`를 `<script>`, `<link>`, 동적 script 삽입으로 불러오는 방식을 금지한다.
- 핫픽스 파일을 뒤에 덧씌워 기존 함수를 재정의하는 방식을 금지한다.

## 2. 업데이트 원칙
- 업데이트 시 이전 운영본의 완성 기능을 신버전 소스에 직접 이관한다.
- 수정사항은 신버전의 원본 함수/컴포넌트/스타일 안에서 반영한다.
- 동일 기능을 여러 버전 래퍼가 연속으로 감싸지 않는다.
- 이전 버전 파일은 보관용일 수 있으나 운영 경로에서 참조하지 않는다.

## 3. 기능 유지 원칙
- 안정화 과정에서 현재 완성된 사용자 기능과 저장 데이터 구조는 원칙적으로 유지한다.
- 기능 제거는 사용 여부와 영향도를 확인한 뒤 별도 결정한다.
- 죽은 코드, 도달 불가능 코드, 최신 구현에 완전히 대체된 보정 코드는 제거한다.

## 4. 권한/저장 로직 원칙
- 역할별 권한은 하나의 판정 경로를 사용한다.
- 동일 작업에 구형 API와 신형 API가 동시에 존재하지 않도록 정리한다.
- 자기정보 수정, 회원 수정, 모임 전환 등 핵심 작업은 회원 ID와 모임 ID를 기준으로 처리한다.

## 5. 배포 전 자동검사
신버전 배포 전 최소 다음을 확인한다.
1. 신버전 HTML에서 구버전 JS/CSS 참조 0건
2. 신버전 JS 내부에서 구버전 app 파일 동적 로드 0건
3. JavaScript 문법 검사 통과
4. 로그인/회원명부/회원수정/게임대기/편성대기/게임중/모임전환 기본 QA 통과
5. iPhone/Kakao WebKit 핵심 터치 QA 통과

## 6. 버전 정책
- `v6.0`을 누적형 구조를 끝내는 독립 운영본 기준점으로 사용한다.
- 이후 `v6.1`, `v6.2` 등은 직전 독립 운영본을 기준으로 전체 소스를 갱신하되, 이전 버전 파일을 런타임에 불러오지 않는다.
