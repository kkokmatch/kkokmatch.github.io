# 콕매치 안정화 개발 원칙

## 1. 운영본 독립 원칙
- 운영 중인 버전은 자기 버전의 JS/CSS만 로드한다.
- 운영 HTML에는 **현재 버전의 앱 JS 1개와 앱 CSS 1개만** 연결한다.
- 신버전이 구버전 `app-vXX.js`, `app-vXX.css`를 `<script>`, `<link>`, 동적 script 삽입으로 불러오는 방식을 금지한다.
- 별도 `hotfix.js`, `hotfix.css`, `fixXX.js`를 운영본 뒤에 덧씌워 기존 함수를 재정의하는 방식을 금지한다.
- v6.21부터 `app-v<현재버전>.js` + `app-v<현재버전>.css`를 유일한 운영 런타임으로 사용한다.

## 2. 업데이트 원칙
- 모든 수정은 현재 단일본을 기준으로 다음 단일본에 직접 반영한다.
- 수정사항은 신버전의 원본 함수, 컴포넌트, 스타일 안에서 반영하고 런타임 패치 파일을 추가하지 않는다.
- 동일 기능을 여러 버전 래퍼가 연속으로 감싸지 않는다. 새 구현이 기존 구현을 완전히 대체하면 기존 구현을 제거한다.
- 이전 버전은 Git 커밋/백업 브랜치로 보존하며, 운영 경로에서 과거 앱 파일을 참조하지 않는다.
- 기능 하나를 고칠 때 해당 기능과 충돌하는 구형 보정, 중복 이벤트 리스너, 사용되지 않는 helper/bridge/loader가 확인되면 영향도를 확인한 뒤 같은 변경에서 함께 제거한다.
- 영향 범위가 로그인, 권한, 저장 구조, 게임편성 등 핵심 기능 전체로 확대되는 정리는 자동으로 대량 삭제하지 않고 별도 안정화 작업으로 분리한다.

## 3. 안정화 최우선 원칙
- 새 기능 추가보다 기존 기능의 예측 가능한 동작, 데이터 보존, 모바일 안정성을 우선한다.
- 화면이 처음에는 정상이나 클릭/스크롤/상태변경 후 다시 변하는 현상은 임시 CSS 보정보다 중복 렌더/이벤트/상태변경 소스 충돌을 먼저 제거한다.
- iPhone Safari와 카카오톡 인앱브라우저를 핵심 지원 환경으로 보고 터치 이동과 탭을 구분한다.
- 상태 변경 후 전체 화면을 불필요하게 재렌더하지 않되, 부분 갱신 경로가 canonical 상태와 달라지는 별도 로직을 만들지 않는다.
- 현재 완성된 사용자 기능과 저장 데이터 구조는 원칙적으로 유지한다. 기능 제거는 사용 여부와 영향도를 확인한 뒤 결정한다.

## 4. 죽은 코드 정리 원칙
다음 코드는 발견 시 제거 대상이다.
1. 현재 진입 경로에서 도달할 수 없는 `/launch/vXX` 전용 코드
2. 이미 본체에 이관된 파일을 다시 불러오는 동적 bridge/loader
3. 최신 canonical handler가 완전히 대체한 과거 상태변경/렌더/저장 보정 코드
4. 호출처가 없고 부작용도 없는 함수, 상수, DOM 보정 코드
5. 현재 버전보다 오래된 QA만을 위해 운영 런타임에 남아 있는 호환 코드
6. 같은 DOM이나 상태를 서로 다른 순서로 다시 쓰는 중복 MutationObserver/이벤트 리스너

단, 삭제 전 현재 호출 여부와 부작용을 확인하고 핵심 기능에 영향 가능성이 크면 별도 검증 후 제거한다.

## 5. 권한/저장 로직 원칙
- 역할별 권한은 하나의 판정 경로를 사용한다.
- 동일 작업에 구형 API와 신형 API가 동시에 존재하지 않도록 정리한다.
- 자기정보 수정, 회원 수정, 모임 전환 등 핵심 작업은 이름이 아니라 회원 ID와 모임 ID를 기준으로 처리한다.
- 화면 이벤트도 동일 기능에 대해 하나의 canonical handler만 사용한다.
- 프런트 중복검사와 서버 중복검사 기준은 반드시 동일하게 유지한다.
- 개발자/모임장/운영진 등의 역할값은 일반 프로필 필드 변경 때문에 의도치 않게 강등되거나 차단되지 않도록 권한과 프로필 수정을 분리한다.

## 6. 신버전 작성 절차
1. 현재 운영 단일본의 완성 기능과 데이터/API 계약을 기준선으로 확정한다.
2. 직전 단일본을 복제해 새 버전의 JS/CSS 각각 1개를 만든다.
3. 변경사항을 새 버전 내부에 직접 수정한다.
4. 대체된 구형 코드, 죽은 코드, 중복 보정을 함께 정리한다.
5. 구버전 파일 로드, 구버전 함수용 동적 asset 삽입, 별도 hotfix 로드가 없는지 검사한다.
6. JavaScript 문법검사와 현재버전 스모크 QA를 통과시킨다.
7. 핵심 기능 QA 통과 후에만 `index.html`과 `latest-version.json`을 새 버전으로 전환한다.
8. QA 실패 시 현재 운영본을 유지하고 실패한 신버전은 운영 경로에 연결하지 않는다.

## 7. 배포 전 자동검사
신버전 배포 전 최소 다음을 확인한다.
1. 운영 HTML의 현재버전 JS 1개, CSS 1개
2. 현재버전 JS 내부에서 다른 `/app-v*.js`, `/app-v*.css` 로드 0건
3. JavaScript 문법 검사 통과
4. 로그인 화면 초기화 및 콘솔 page error 0건
5. 회원명부 주요 상태 버튼 위치/크기와 상태전환 QA 통과
6. 회원수정/동명이인/권한별 수정 저장 QA 통과
7. 게임대기/편성대기/게임중/모임전환 기본 QA 통과
8. 모바일 390px급 화면과 iPhone/Kakao WebKit 핵심 터치 QA 통과
9. 현재 버전 외 app 자산 네트워크 요청 0건

## 8. QA 및 워크플로 관리
- 자동 QA는 `latest-version.json`을 기준으로 현재 운영 버전을 검사하도록 작성한다.
- 특정 과거 버전 번호를 하드코딩한 QA는 현재 기능 검증에 사용하지 않는다.
- 현재 QA로 완전히 대체된 과거 버전 전용 workflow는 제거해 false failure와 유지보수 비용을 줄인다.
- 구조 검사는 `scripts/validate-standalone-runtime.mjs`를 기준으로 유지한다.
- 모바일 핵심 흐름은 `scripts/qa-current-runtime.mjs`처럼 현재 단일본을 직접 띄워 검증한다.

## 9. 버전/롤백 정책
- v6.21을 **단일본 운영 구조의 기준점**으로 사용한다.
- 이후 버전은 직전 단일본을 기준으로 `app-v<새버전>.js` + `app-v<새버전>.css`를 만든다.
- 버전 상승은 기능 수정 + 죽은 코드 정리 + QA가 한 묶음으로 완료됐을 때 적용한다.
- 롤백은 운영 HTML에서 오래된 파일을 상시 병행 로드하는 방식이 아니라 Git 커밋/백업 브랜치로 수행한다.
- 과거 파일은 롤백 근거로만 보관할 수 있으며 현재 런타임 의존성으로 사용하지 않는다.
