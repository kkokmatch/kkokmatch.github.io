name: Bump v5.6 hotfix loader

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Manual v5.6 loader maintenance
        run: |
          echo "Automatic index rewriting is disabled."
          echo "Run only after explicit review of the mobile startup path."
