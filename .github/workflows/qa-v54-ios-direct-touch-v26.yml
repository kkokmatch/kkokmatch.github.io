name: Legacy QA v5.4 iOS direct touch (manual only)
on:
  workflow_dispatch:
jobs:
  legacy-webkit-direct-touch:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Legacy v5.4 workflow is retired."
          echo "Use Prune Kokmatch v6 runtime and Validate standalone runtime for current QA."
